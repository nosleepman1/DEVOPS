# USING FRONT BACK AND DB

 1. frontend Dockerfile on ./frontend
 2. backend Dockerfile on ./backend
 3. Docker-compose in the working tree

## How to use it

```bash
git clone https://github.com/nosleepman1/DEVOPS.git
cd docker with react-laravel
docker-compose up -d --build
```